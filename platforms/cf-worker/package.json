{
  "name": "fuzd-cf-worker",
  "version": "0.6.0",
  "publishConfig": {
    "access": "public"
  },
  "type": "module",
  "files": [
    "src",
    "tsconfig.json"
  ],
  "dependencies": {
    "named-logs": "^0.3.0",
    "named-logs-context": "^0.1.1",
    "remote-sql-d1": "^0.0.5",
    "workers-logger": "npm:@bug-fix/workers-logger"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.22",
    "@cloudflare/workers-types": "^4.20241022.0",
    "@types/node": "^22.7.9",
    "fuzd-chain-protocol": "workspace:*",
    "fuzd-common": "workspace:*",
    "fuzd-executor": "workspace:*",
    "fuzd-scheduler": "workspace:*",
    "fuzd-server": "workspace:*",
    "hono": "^4.6.6",
    "ldenv": "^0.3.10",
    "remote-account": "^0.2.1",
    "set-defaults": "^0.0.2",
    "typescript": "^5.6.3",
    "viem": "^2.21.35",
    "vitest": "^2.1.3",
    "wrangler": "^3.83.0"
  },
  "scripts": {
    "prepare": "set-defaults .",
    "deploy": "wrangler deploy --minify",
    "dev": "wrangler dev -e production --port 34002",
    "init-db": "ldenv -m local wrangler d1 execute fuzd-db --@@MODE --file=node_modules/fuzd-server/src/schema/sql/scheduler.sql ~~ wrangler d1 execute fuzd-db --@@MODE --file=node_modules/fuzd-server/src/schema/sql/executor.sql ~~ @@",
    "test": "vitest"
  }
}